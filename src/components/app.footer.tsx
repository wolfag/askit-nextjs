import React from 'react';

type Props = {};

export default function AppFooter({}: Props) {
  return (
    <div
      style={{
        background: '#ddd',
        textAlign: 'center',
        padding: '5px 0',
      }}
    >
      AppFooter
    </div>
  );
}
